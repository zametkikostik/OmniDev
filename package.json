{
  "name": "omnidev",
  "version": "0.2.0",
  "description": "OmniDev — Autonomous AI Full-Stack Application Builder",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@omnidev/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
